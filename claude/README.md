# Claude Assets

This directory contains Claude-specific assets that the installer places into `~/.claude/`.

- `agents/` contains Claude subagent markdown files.
- `hooks/` contains Claude hook scripts.

User-invocable commands are skills under the repository's `skills/` directory.
Claude-specific structured settings are still merged through `config/claude-hooks.json`.
